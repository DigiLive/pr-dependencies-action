name: 'PR Dependency Check'
description: 'Verify PR dependencies are merged or closed'
author: 'Ferry Cools'
branding:
  icon: alert-triangle
  color: orange
runs:
  using: 'node20'
  main: 'dist/main.js'
inputs:
  phrases:
    description: 'Pipe-separated list of key phrases to identify dependency declarations'
    required: false
    default: 'depends on|blocked by'
  label:
    description: 'Label to add to PR when dependencies are found'
    required: false
    default: 'blocked'
outputs:
  has-dependencies:
    description: 'Whether any dependencies were found'
